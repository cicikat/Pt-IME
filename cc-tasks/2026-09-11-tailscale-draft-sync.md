# Tailscale 草稿同步兼容

用户明确手机和电脑均使用 Tailscale，后端接收路径 `/v1/ime/drafts`。

- [x] 网络策略接受 Tailscale 使用的共享地址段 100.64.0.0/10，保留 HTTPS 系统证书、独立配对、禁止代理与重定向。
- [x] 更新相关网络测试、协议和设置提示；构建并独立提交。
- [ ] 真机安装和配对上报验收（需用户设备）。

不更改采集规则、默认关闭和用户主动同意流程，不内置地址或密钥。
