# v1 隐私说明

## 可选编辑观察

开启草稿记录后，除输入历史外也记录有界的删除、拼音组合删除、清空与恢复事件。开启回传后发送给配置的收件端；收件端可另开活动理解与角色主动关心。数字遮蔽不是完整匿名化，敏感输入框与锁屏继续排除。


没有开发者服务器、账号系统、广告 SDK 或自动云同步。拼音、语音离线运行。

## 本地保存

拼音偏好保存静态词库 ID、次数和版本，不把完整输入句子写入用户词库。用户维护的短语、主题、布局及 Emoji/符号最近使用保存在应用私有空间。

剪贴板历史保存完整内容，最多 50 条未固定记录，固定项另计；敏感编辑器期间跳过。剪贴板历史独立于草稿开关，不应用数字遮蔽。

三小时草稿默认关闭。开启后保存已提交文本、来源 App、时间和键盘/语音来源，仅 ASCII/全角阿拉伯数字替换为星号。关闭不删除已有历史，可在最近输入页清除。到期从最后更新计，运行期间每分钟清理，读取/发送前也清理；进程停止时无法准点清理，SQLite 删除不保证物理擦除闪存残留。

## 回传

回传另有默认关闭开关；首次升级 v1 同样要求重新开启草稿并确认回传。需填写完整 HTTP/HTTPS 接口地址和密钥并保存，主动确认后才自动发送。默认只允许私网、回环和 Tailscale 地址；公网穿透需手动开启“允许公网地址”。HTTP 不加密，链路上的人可能读取密钥和草稿；HTTPS 不跳过证书校验。不跟随重定向，不使用系统代理。域名解析使用系统 DNS，可能产生 DNS 网络请求。

接收端能读取非数字文本、来源 App 和时间。手机清空不发远程删除，关闭也不能撤回已经发出的请求。服务端应独立配置保留时间，避免记录请求正文到日志，给每台手机发独立可撤销密钥。

密钥使用 Android Keystore 的 AES-GCM 加密存储；它不随 APK 分发。应用沙箱不能抵御 root、恶意系统或已被攻陷的手机。

## 敏感字段与语音

按 Android inputType 排除 password、visible/web password、email/web email、number password，并尊重禁止个性化学习的标记。普通聊天框里的姓名、住址、密码、支付信息或中文数字无法可靠自动识别。不要把数字遮蔽当作通用脱敏保护。

录音只在语音会话期间启用，PCM 仅在内存中处理，不保存音频文件、不传给第三方。取消/切换输入框关闭录音并忽略迟到结果，模型闲置 60 秒释放。只有 final 成功上屏且草稿开启时才记录一次。

## 分享和升级

只分享 APK 不会包含应用私有数据库、配置、密钥。公开 Release 不可调试，禁用应用备份/设备迁移。分享配置或应用数据备份不同于分享 APK，请勿共享密钥。

早期 Debug 和官方 Release 签名不同，不能直接覆盖。卸载清除私有数据；目前不提供全量迁移工具，请自行保存所需内容。


### Tailscale pairing (2026-09-11)

Private-network sync permits Tailscale's 100.64.0.0/10 range. HTTP is supported with a plaintext warning; HTTPS keeps certificate validation. Public tunnel destinations require explicit opt-in. Neither protocol follows redirects or uses a system proxy.
